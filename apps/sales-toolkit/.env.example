# Copy this file to .env.local (git-ignored) and fill in with your credentials.
# The toolkit backend reads these values via process.env.*.

# Primary chat/JSON model credentials (OpenAI or compatible provider).
OPENAI_API_KEY=
# Optional overrides; defaults are shown in README if left blank.
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_EMBEDDINGS_MODEL=text-embedding-3-small

# xAI Grok support (AutoBrief route prefers these if present)
# XAI_API_KEY=
# XAI_BASE_URL=https://api.x.ai/v1
# XAI_MODEL=grok-code-fast-1
